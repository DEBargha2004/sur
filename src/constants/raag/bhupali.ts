import { Sur, Translation } from "@/schema/sur";

export const bhupali: Record<Translation, Sur[]> = {
  aroha: ["সা", "রে", "গা", "পা", "ধা", "সাঁ"],
  avoroha: ["সাঁ", "ধা", "পা", "গা", "রে", "সা"],
};
